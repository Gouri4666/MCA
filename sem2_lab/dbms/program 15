SET SERVEROUTPUT ON
SET VERIFY OFF

DECLARE
    n1 NUMBER := &num1;
    n2 NUMBER := &num2;
BEGIN
	IF n1<n2 THEN
		DBMS_OUTPUT.PUT_LINE('Maximun: ' || n2);
	ELSE
		DBMS_OUTPUT.PUT_LINE('Maximum: ' || n1);
	END IF;
END;
/
